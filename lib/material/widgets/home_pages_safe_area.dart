import 'package:flutter/material.dart';

class HomePagesSafeArea extends StatelessWidget {
  const HomePagesSafeArea({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 120,
    );
  }
}
